<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guang.resms.module.user.mapper.PermissionMapper">

    <!-- 根据用户ID获取所有权限 -->
    <select id="selectPermissionsByUserId" resultType="com.guang.resms.module.user.entity.Permission">
        SELECT DISTINCT p.*, p.permission_type
        FROM tb_permission p
                 INNER JOIN tb_role_permission rp ON p.id = rp.permission_id
                 INNER JOIN tb_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND p.status = 1  -- 启用状态
        ORDER BY p.permission_type, p.sort_order
    </select>

</mapper>
