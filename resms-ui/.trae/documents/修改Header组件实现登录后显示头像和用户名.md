我需要修改Header组件，使其根据登录状态动态显示内容。具体实现如下：

1. **导入用户store**：在Header组件中导入useUserStore，以便获取登录状态和用户信息
2. **添加条件渲染**：修改模板中的user-area部分，使用v-if和v-else根据isLoggedIn状态显示不同内容
3. **登录状态下显示**：显示用户头像（使用el-avatar组件）和用户名
4. **添加退出登录功能**：为登录状态的用户添加退出登录按钮
5. **样式调整**：确保新增的头像和用户名样式与现有设计一致

修改后的Header组件将能够根据用户登录状态自动切换显示内容，提供更好的用户体验。